import colors from "./colors.scss";

// Loads the colors.scss values into Javascript so that they can be reused

// import  { Colors } from "../colors";
// color= {Colors.primary}

export const Colors = colors;